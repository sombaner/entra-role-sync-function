{
  "name": "entra-role-sync-function",
  "version": "1.0.0",
  "private": true,
  "description": "Timer-triggered Azure Function that precomputes extensionAttribute2 for SCIM provisioning based on Entra app role membership.",
  "main": "src/app.js",
  "scripts": {
    "start": "func start",
    "test:syntax": "node --check src/app.js && node --check src/config.js && node --check src/graphClient.js && node --check src/roleSyncTimer.js"
  },
  "engines": {
    "node": ">=20"
  },
  "dependencies": {
    "@azure/functions": "^4.5.0",
    "@azure/identity": "^4.4.1",
    "@microsoft/microsoft-graph-client": "^3.0.7",
    "isomorphic-fetch": "^3.0.0"
  }
}